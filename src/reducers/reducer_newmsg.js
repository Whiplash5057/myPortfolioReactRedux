

export default function(state = null, action) {
    switch (action.type) {
      case 'NEW_MESSAGE':
        return state;
        break;
      default:
        return state;
    }
  
    return state;
  }